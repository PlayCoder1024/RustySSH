# Issues

1. there may no '$EDITOR' environment or 'vi' in user's computer, implement a way to detect the usable editor.
2. prompt to enter password when auth type is password, and save password properly.
3. when connect to server, can not type anything...Should support the input and output to remote server after connected.
4. when type 'ctrl c' after connected to remote, the whole RusttySSH quit, fix the bug.

# Issues(0113)

1. if using jump proxy, after the first password input, the whole app hung, fix the bug.
